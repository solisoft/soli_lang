# Benchmarks: Soli vs Ruby

Reproducible per-operation numbers comparing Soli's production engine against Ruby. The
scripts ship in `bench/cross-language/` so you can re-run them yourself.

> **Read this first.** Benchmarks are easy to rig and easy to get wrong — this page has
> already been corrected once (see [Corrections](#corrections)). Everything below is
> best-of-7 on **byte-identical inputs** generated by the same LCG in both languages, and
> Ruby is credited with its **best** result across three modes, not its slowest. Where Soli
> loses, the number is printed exactly as measured.

## Setup

| | |
|---|---|
| Soli | production bytecode VM (`soli --vm`) |
| Ruby | 4.0.6 — default, `--yjit`, `--zjit` |
| Method | best-of-7 per operation, warmed once first |
| Data | n = 20,000; identical LCG-generated inputs in both languages |
| Machine | idle 16-core x86-64 Linux |

`ratio` is Soli ÷ Ruby's *best* mode. **Below 1.00 means Soli is faster.**

## Summary

| Category | Geometric mean | Soli wins |
|---|---:|---:|
| Aggregate | 0.42x — Soli **2.4x faster** | 5/5 |
| String | 0.35x — Soli **2.9x faster** | 11/13 |
| Array | 1.16x — Soli 1.2x slower | 7/16 |
| DateTime | 2.57x — Soli 2.6x slower | 1/9 |
| Hash | 2.20x — Soli 2.2x slower | 1/11 |
| Duration | 9.92x — Soli 9.9x slower | 0/2 |
| Numeric | 3.88x — Soli 3.9x slower | 0/3 |
| Control | 3.86x — Soli 3.9x slower | 0/2 |
| **Overall** | **1.24x** | **25/61** |

The split is not random — it tracks **where the work happens**:

- **Inside Rust** — string transforms, `flatten`, `sort`, `DateTime.parse`, and the whole
  Aggregate family — Soli wins, often by 3–30x.
- **Per-element in interpreted code** — arithmetic loops, function calls, blocks, hash
  iteration — Ruby wins by 2–4x, because its JIT compiles those loops to machine code.

The practical lesson is in the Aggregate row. `rows.sum_by("amount")` and
`rows.reduce(fn(a, r) { return a + r["amount"] }, 0)` compute the same thing, but the first
stays in Rust and the second re-enters the interpreter 20,000 times — a **235x** difference,
and the difference between beating Ruby and losing to it. Prefer the built-in.

## Aggregate

Field-keyed aggregates: `rows.sum_by("amount")` rather than `rows.reduce(fn(a, r) ...)`. Because the field name is data rather than a callback, the whole traversal stays in Rust and never re-enters the interpreter. Ruby has no non-block form of these, so its versions pay a block call per element — which is precisely the cost being avoided.

| Operation | Soli | Ruby | +YJIT | +ZJIT | Ratio |
|---|---:|---:|---:|---:|---:|
| `sum_by` | 0.299 | 1.101 | 1.091 | 0.979 | **0.31x** |
| `index_by` | 0.779 | 2.314 | 2.173 | 2.192 | **0.36x** |
| `count_by` | 0.803 | 2.517 | 2.139 | 2.152 | **0.38x** |
| `group_by` | 0.993 | 1.839 | 1.793 | 1.777 | **0.56x** |
| `tally` | 0.185 | 0.406 | 0.321 | 0.408 | **0.58x** |

## String

Soli's strings are Rust `EcoString`s and the methods are native code with no per-element interpretation.

| Operation | Soli | Ruby | +YJIT | +ZJIT | Ratio |
|---|---:|---:|---:|---:|---:|
| `capitalize` | 0.019 | 0.278 | 0.279 | 0.275 | **0.07x** |
| `downcase` | 0.003 | 0.034 | 0.034 | 0.034 | **0.10x** |
| `upcase` | 0.004 | 0.029 | 0.029 | 0.028 | **0.14x** |
| `replace_all` | 0.064 | 0.466 | 0.458 | 0.457 | **0.14x** |
| `contains` | 0.003 | 0.014 | 0.013 | 0.013 | **0.20x** |
| `concat_plus` | 1.323 | 4.915 | 5.148 | 4.888 | **0.27x** |
| `bytes` | 0.129 | 0.445 | 0.436 | 0.422 | **0.30x** |
| `chars` | 1.139 | 2.895 | 2.650 | 2.750 | **0.43x** |
| `sub` | 0.002 | 0.005 | 0.006 | 0.005 | **0.44x** |
| `split` | 0.237 | 0.284 | 0.289 | 0.274 | **0.87x** |
| `index_of` | 0.012 | 0.014 | 0.013 | 0.013 | **0.91x** |
| `interpolate` | 1.923 | 1.664 | 1.548 | 1.574 | 1.24x |
| `reverse` | 0.047 | 0.018 | 0.017 | 0.017 | 2.79x |

## Array

Mixed. Soli wins the bulk native transforms; Ruby wins anything that calls a block per element.

| Operation | Soli | Ruby | +YJIT | +ZJIT | Ratio |
|---|---:|---:|---:|---:|---:|
| `flatten` | 0.165 | 0.868 | 0.807 | 0.800 | **0.21x** |
| `join` | 0.168 | 0.432 | 0.416 | 0.348 | **0.48x** |
| `union` | 0.686 | 1.365 | 1.389 | 1.399 | **0.50x** |
| `sum` | 0.007 | 0.013 | 0.013 | 0.013 | **0.59x** |
| `includes` | 0.038 | 0.051 | 0.050 | 0.050 | **0.75x** |
| `index_of` | 0.038 | 0.051 | 0.050 | 0.050 | **0.75x** |
| `sort` | 1.106 | 1.331 | 1.312 | 1.308 | **0.85x** |
| `reverse` | 0.069 | 0.059 | 0.056 | 0.059 | 1.22x |
| `intersection` | 1.148 | 0.824 | 0.821 | 0.851 | 1.40x |
| `uniq` | 0.498 | 0.357 | 0.336 | 0.343 | 1.48x |
| `reduce` | 1.079 | 0.762 | 0.784 | 0.682 | 1.58x |
| `map` | 0.937 | 0.647 | 0.501 | 0.622 | 1.87x |
| `difference` | 1.514 | 0.731 | 0.741 | 0.751 | 2.07x |
| `build` | 1.071 | 0.526 | 0.481 | 0.488 | 2.23x |
| `filter` | 1.285 | 0.653 | 0.564 | 0.642 | 2.28x |
| `each` | 0.859 | 0.477 | 0.084 | 0.466 | 10.23x |

## DateTime

One enormous Soli win (`parse`, Ruby's `Time.parse` is famously slow) against consistent losses elsewhere — every Soli DateTime operation allocates an object.

| Operation | Soli | Ruby | +YJIT | +ZJIT | Ratio |
|---|---:|---:|---:|---:|---:|
| `parse` | 4.608 | 153.059 | 135.576 | 145.348 | **0.03x** |
| `format` | 7.045 | 5.898 | 5.903 | 5.894 | 1.20x |
| `subtract_days` | 3.502 | 0.829 | 0.863 | 0.808 | 4.33x |
| `add_hours` | 3.561 | 0.833 | 0.832 | 0.793 | 4.49x |
| `now` | 5.104 | 1.132 | 1.082 | 1.091 | 4.72x |
| `to_unix` | 2.245 | 0.454 | 0.428 | 0.426 | 5.27x |
| `end_of_month` | 9.620 | 1.892 | 1.790 | 1.844 | 5.37x |
| `from_unix` | 4.033 | 0.712 | 0.674 | 0.724 | 5.98x |
| `year` | 3.192 | 0.425 | 0.409 | 0.417 | 7.80x |

## Hash

Ruby leads. Both use open-addressing maps with fast hashers, so the gap is the per-iteration interpreter cost around them, not the data structure.

| Operation | Soli | Ruby | +YJIT | +ZJIT | Ratio |
|---|---:|---:|---:|---:|---:|
| `build` | 2.612 | 3.848 | 3.728 | 3.840 | **0.70x** |
| `merge` | 0.714 | 0.753 | 0.712 | 0.721 | 1.00x |
| `has_key` | 1.174 | 1.037 | 1.006 | 1.079 | 1.17x |
| `set` | 1.279 | 1.086 | 1.032 | 1.048 | 1.24x |
| `get` | 1.139 | 0.921 | 0.885 | 0.848 | 1.34x |
| `invert` | 0.770 | 0.481 | 0.437 | 0.450 | 1.76x |
| `each` | 1.112 | 0.515 | 0.466 | 0.491 | 2.39x |
| `transform_values` | 1.608 | 0.566 | 0.494 | 0.571 | 3.26x |
| `select` | 2.208 | 0.849 | 0.803 | 0.551 | 4.01x |
| `values` | 0.070 | 0.014 | 0.014 | 0.014 | 5.11x |
| `keys` | 0.152 | 0.010 | 0.011 | 0.013 | 15.49x |

## Duration

**Not like-for-like.** Ruby has no Duration type, so this compares Soli's `Duration` objects against integer-second arithmetic. Shown for completeness; do not quote it.

| Operation | Soli | Ruby | +YJIT | +ZJIT | Ratio |
|---|---:|---:|---:|---:|---:|
| `between` | 5.092 | 1.002 | 0.940 | 0.956 | 5.42x |
| `of_days` | 4.215 | 0.289 | 0.232 | 0.267 | 18.16x |

## Numeric

Ruby's clearest win. Tight arithmetic loops are dominated by opcode dispatch, where Ruby's JIT-compiled loops show.

| Operation | Soli | Ruby | +YJIT | +ZJIT | Ratio |
|---|---:|---:|---:|---:|---:|
| `float_math` | 1.654 | 0.559 | 0.561 | 0.632 | 2.96x |
| `int_loop` | 0.833 | 0.264 | 0.263 | 0.290 | 3.17x |
| `modulo` | 1.984 | 0.323 | 0.319 | 0.364 | 6.22x |

## Control

Function and closure calls. YJIT/ZJIT compile these to machine code; Soli interprets every call.

| Operation | Soli | Ruby | +YJIT | +ZJIT | Ratio |
|---|---:|---:|---:|---:|---:|
| `closure_call` | 1.703 | 0.614 | 0.525 | 0.584 | 3.24x |
| `fn_call` | 1.888 | 0.565 | 0.414 | 0.411 | 4.60x |

## Why the split exists

Soli's built-in methods are compiled Rust over native data structures, so an operation that
never re-enters the interpreter runs at Rust speed. An operation that calls back into Soli
once per element pays ~44 cycles per bytecode dispatch, and Ruby's JIT compiles the
equivalent loop to machine code — an entire ZJIT iteration fits inside one Soli dispatch.

That is the whole story of this page. It is also why the fix for a slow Soli program is
usually to reach for a built-in rather than to hand-write the loop.

## What this does not measure

Micro-benchmarks of individual operations. They say nothing about end-to-end web request
throughput, where framework architecture, the database and I/O dominate. Treat this as a
guide to *which operations are cheap*, not a verdict on either language.

## Corrections

This page's first version reported an overall **0.99x**. That was wrong: the input data
flattered Soli. `sort` ran on already-sorted input (Rust detects the ordered run and goes
O(n); Ruby does not), the set operations ran on two identical arrays, `flatten` on a flat
array, and `uniq` on all-unique input. All are fixed — shuffled LCG data, 50%-overlapping
ranges, genuinely nested input, duplicate-heavy input — and `sort`, which had looked like a
5x win, is a real but modest one.

## Reproducing

```bash
soli --vm bench/cross-language/bench_all.sl
ruby        bench/cross-language/bench_all.rb   # add --yjit / --zjit
```

Both print `category|operation|best_ms`.
