<div class="max-w-4xl mx-auto">
    <!-- Hero Section -->
    <div class="mb-16">
        <h1 class="text-4xl font-bold tracking-tight text-white sm:text-5xl mb-6">
            <span class="bg-gradient-to-r from-indigo-400 via-purple-400 to-pink-400 bg-clip-text text-transparent">Built-in Functions Reference</span>
        </h1>
        <p class="text-xl text-gray-300 leading-8">
            Complete reference for all 150+ built-in functions in Soli. From I/O and string manipulation to HTTP requests, cryptography, and testing utilities.
        </p>
    </div>

    <!-- Quick Navigation -->
    <nav class="not-prose mb-16">
        <h2 class="text-lg font-semibold text-white mb-4 flex items-center gap-2">
            <svg class="w-5 h-5 text-indigo-400" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16" />
            </svg>
            Quick Navigation
        </h2>
        <div class="grid grid-cols-2 sm:grid-cols-3 lg:grid-cols-4 gap-3">
            <a href="#core" class="flex items-center gap-2 p-3 rounded-lg bg-white/5 border border-white/10 hover:border-indigo-500/50 hover:bg-white/10 transition-all group">
                <span class="w-8 h-8 rounded bg-blue-500/20 flex items-center justify-center text-blue-400 text-xs font-bold">C</span>
                <span class="text-gray-300 group-hover:text-white text-sm">Core</span>
            </a>
            <a href="#http" class="flex items-center gap-2 p-3 rounded-lg bg-white/5 border border-white/10 hover:border-indigo-500/50 hover:bg-white/10 transition-all group">
                <span class="w-8 h-8 rounded bg-green-500/20 flex items-center justify-center text-green-400 text-xs font-bold">H</span>
                <span class="text-gray-300 group-hover:text-white text-sm">HTTP</span>
            </a>
            <a href="#json" class="flex items-center gap-2 p-3 rounded-lg bg-white/5 border border-white/10 hover:border-indigo-500/50 hover:bg-white/10 transition-all group">
                <span class="w-8 h-8 rounded bg-amber-500/20 flex items-center justify-center text-amber-400 text-xs font-bold">J</span>
                <span class="text-gray-300 group-hover:text-white text-sm">JSON</span>
            </a>
            <a href="#crypto" class="flex items-center gap-2 p-3 rounded-lg bg-white/5 border border-white/10 hover:border-indigo-500/50 hover:bg-white/10 transition-all group">
                <span class="w-8 h-8 rounded bg-red-500/20 flex items-center justify-center text-red-400 text-xs font-bold">Cr</span>
                <span class="text-gray-300 group-hover:text-white text-sm">Crypto</span>
            </a>
            <a href="#jwt" class="flex items-center gap-2 p-3 rounded-lg bg-white/5 border border-white/10 hover:border-indigo-500/50 hover:bg-white/10 transition-all group">
                <span class="w-8 h-8 rounded bg-purple-500/20 flex items-center justify-center text-purple-400 text-xs font-bold">Jw</span>
                <span class="text-gray-300 group-hover:text-white text-sm">JWT</span>
            </a>
            <a href="#regex" class="flex items-center gap-2 p-3 rounded-lg bg-white/5 border border-white/10 hover:border-indigo-500/50 hover:bg-white/10 transition-all group">
                <span class="w-8 h-8 rounded bg-pink-500/20 flex items-center justify-center text-pink-400 text-xs font-bold">Rx</span>
                <span class="text-gray-300 group-hover:text-white text-sm">Regex</span>
            </a>
            <a href="#env" class="flex items-center gap-2 p-3 rounded-lg bg-white/5 border border-white/10 hover:border-indigo-500/50 hover:bg-white/10 transition-all group">
                <span class="w-8 h-8 rounded bg-cyan-500/20 flex items-center justify-center text-cyan-400 text-xs font-bold">E</span>
                <span class="text-gray-300 group-hover:text-white text-sm">Env</span>
            </a>
            <a href="#datetime" class="flex items-center gap-2 p-3 rounded-lg bg-white/5 border border-white/10 hover:border-indigo-500/50 hover:bg-white/10 transition-all group">
                <span class="w-8 h-8 rounded bg-teal-500/20 flex items-center justify-center text-teal-400 text-xs font-bold">Dt</span>
                <span class="text-gray-300 group-hover:text-white text-sm">DateTime</span>
            </a>
            <a href="#validation" class="flex items-center gap-2 p-3 rounded-lg bg-white/5 border border-white/10 hover:border-indigo-500/50 hover:bg-white/10 transition-all group">
                <span class="w-8 h-8 rounded bg-emerald-500/20 flex items-center justify-center text-emerald-400 text-xs font-bold">V</span>
                <span class="text-gray-300 group-hover:text-white text-sm">Validation</span>
            </a>
            <a href="#session" class="flex items-center gap-2 p-3 rounded-lg bg-white/5 border border-white/10 hover:border-indigo-500/50 hover:bg-white/10 transition-all group">
                <span class="w-8 h-8 rounded bg-orange-500/20 flex items-center justify-center text-orange-400 text-xs font-bold">S</span>
                <span class="text-gray-300 group-hover:text-white text-sm">Session</span>
            </a>
            <a href="#testing" class="flex items-center gap-2 p-3 rounded-lg bg-white/5 border border-white/10 hover:border-indigo-500/50 hover:bg-white/10 transition-all group">
                <span class="w-8 h-8 rounded bg-lime-500/20 flex items-center justify-center text-lime-400 text-xs font-bold">T</span>
                <span class="text-gray-300 group-hover:text-white text-sm">Testing</span>
            </a>
            <a href="#i18n" class="flex items-center gap-2 p-3 rounded-lg bg-white/5 border border-white/10 hover:border-indigo-500/50 hover:bg-white/10 transition-all group">
                <span class="w-8 h-8 rounded bg-violet-500/20 flex items-center justify-center text-violet-400 text-xs font-bold">I</span>
                <span class="text-gray-300 group-hover:text-white text-sm">I18n</span>
            </a>
        </div>
    </nav>

    <!-- Core Functions -->
    <section id="core" class="mb-16">
        <h2 class="text-3xl font-bold text-white mb-6 flex items-center gap-3">
            <span class="w-10 h-10 rounded-xl bg-blue-500/20 flex items-center justify-center">
                <svg class="w-5 h-5 text-blue-400" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 10V3L4 14h7v7l9-11h-7z" />
                </svg>
            </span>
            Core Functions
        </h2>

        <!-- I/O Functions -->
        <h3 class="text-xl font-semibold text-white mb-4 mt-8">I/O Functions</h3>

        <div class="space-y-6">
            <div class="rounded-xl bg-white/5 border border-white/10 p-5">
                <div class="flex items-center gap-2 mb-2">
                    <code class="text-lg font-mono text-indigo-400">print(value)</code>
                </div>
                <p class="text-gray-400 mb-3">Prints a value to standard output without a newline.</p>
                <div class="rounded-lg bg-gray-950 ring-1 ring-white/10 p-3 overflow-x-auto">
<pre><code class="language-soli text-sm">print("Hello")
print(" World")  // Output: Hello World</code></pre>
                </div>
            </div>

            <div class="rounded-xl bg-white/5 border border-white/10 p-5">
                <div class="flex items-center gap-2 mb-2">
                    <code class="text-lg font-mono text-indigo-400">println(value)</code>
                </div>
                <p class="text-gray-400 mb-3">Prints a value to standard output with a newline.</p>
                <div class="rounded-lg bg-gray-950 ring-1 ring-white/10 p-3 overflow-x-auto">
<pre><code class="language-soli text-sm">println("Hello World")
println(42)</code></pre>
                </div>
            </div>

            <div class="rounded-xl bg-white/5 border border-white/10 p-5">
                <div class="flex items-center gap-2 mb-2">
                    <code class="text-lg font-mono text-indigo-400">input(prompt?)</code>
                </div>
                <p class="text-gray-400 mb-3">Reads a line of input from the user. Returns the user's input as a String.</p>
                <div class="rounded-lg bg-gray-950 ring-1 ring-white/10 p-3 overflow-x-auto">
<pre><code class="language-soli text-sm">let name = input("Enter your name: ")
println("Hello, " + name)</code></pre>
                </div>
            </div>
        </div>

        <!-- Type Functions -->
        <h3 class="text-xl font-semibold text-white mb-4 mt-8">Type Functions</h3>

        <div class="space-y-6">
            <div class="rounded-xl bg-white/5 border border-white/10 p-5">
                <div class="flex items-center gap-2 mb-2">
                    <code class="text-lg font-mono text-indigo-400">type(value)</code>
                </div>
                <p class="text-gray-400 mb-3">Returns the type name of a value as a string.</p>
                <div class="rounded-lg bg-gray-950 ring-1 ring-white/10 p-3 overflow-x-auto">
<pre><code class="language-soli text-sm">type(42)        // "int"
type("hello")   // "string"
type([1, 2, 3]) // "array"
type(null)      // "null"</code></pre>
                </div>
            </div>

            <div class="rounded-xl bg-white/5 border border-white/10 p-5">
                <div class="flex items-center gap-2 mb-2">
                    <code class="text-lg font-mono text-indigo-400">str(value)</code>
                    <code class="text-lg font-mono text-indigo-400">int(value)</code>
                    <code class="text-lg font-mono text-indigo-400">float(value)</code>
                </div>
                <p class="text-gray-400 mb-3">Convert values between types.</p>
                <div class="rounded-lg bg-gray-950 ring-1 ring-white/10 p-3 overflow-x-auto">
<pre><code class="language-soli text-sm">str(42)       // "42"
int("42")     // 42
int(3.7)      // 3
float("3.14") // 3.14
float(42)     // 42.0</code></pre>
                </div>
            </div>

            <div class="rounded-xl bg-white/5 border border-white/10 p-5">
                <div class="flex items-center gap-2 mb-2">
                    <code class="text-lg font-mono text-indigo-400">len(value)</code>
                </div>
                <p class="text-gray-400 mb-3">Returns the length of a string, array, or hash.</p>
                <div class="rounded-lg bg-gray-950 ring-1 ring-white/10 p-3 overflow-x-auto">
<pre><code class="language-soli text-sm">len("hello")      // 5
len([1, 2, 3])    // 3
len({"a": 1})     // 1</code></pre>
                </div>
            </div>
        </div>

        <!-- Array Functions -->
        <h3 class="text-xl font-semibold text-white mb-4 mt-8">Array Functions</h3>

        <div class="space-y-6">
            <div class="rounded-xl bg-white/5 border border-white/10 p-5">
                <div class="flex items-center gap-2 mb-2">
                    <code class="text-lg font-mono text-indigo-400">push(array, value)</code>
                    <code class="text-lg font-mono text-indigo-400">pop(array)</code>
                </div>
                <p class="text-gray-400 mb-3">Add/remove elements from arrays.</p>
                <div class="rounded-lg bg-gray-950 ring-1 ring-white/10 p-3 overflow-x-auto">
<pre><code class="language-soli text-sm">let arr = [1, 2]
push(arr, 3)     // arr is now [1, 2, 3]
let last = pop(arr)  // last is 3, arr is [1, 2]</code></pre>
                </div>
            </div>

            <div class="rounded-xl bg-white/5 border border-white/10 p-5">
                <div class="flex items-center gap-2 mb-2">
                    <code class="text-lg font-mono text-indigo-400">range(start, end, step?)</code>
                </div>
                <p class="text-gray-400 mb-3">Creates an array of numbers from start to end (exclusive).</p>
                <div class="rounded-lg bg-gray-950 ring-1 ring-white/10 p-3 overflow-x-auto">
<pre><code class="language-soli text-sm">range(0, 5)      // [0, 1, 2, 3, 4]
range(1, 10, 2)  // [1, 3, 5, 7, 9]
range(5, 0, -1)  // [5, 4, 3, 2, 1]</code></pre>
                </div>
            </div>
        </div>

        <!-- Hash Functions -->
        <h3 class="text-xl font-semibold text-white mb-4 mt-8">Hash Functions</h3>

        <div class="space-y-6">
            <div class="rounded-xl bg-white/5 border border-white/10 p-5">
                <div class="flex flex-wrap items-center gap-2 mb-2">
                    <code class="text-lg font-mono text-indigo-400">keys(hash)</code>
                    <code class="text-lg font-mono text-indigo-400">values(hash)</code>
                    <code class="text-lg font-mono text-indigo-400">entries(hash)</code>
                </div>
                <p class="text-gray-400 mb-3">Extract keys, values, or key-value pairs from a hash.</p>
                <div class="rounded-lg bg-gray-950 ring-1 ring-white/10 p-3 overflow-x-auto">
<pre><code class="language-soli text-sm">let h = {"name": "Alice", "age": 30}
keys(h)     // ["name", "age"]
values(h)   // ["Alice", 30]
entries(h)  // [["name", "Alice"], ["age", 30]]</code></pre>
                </div>
            </div>

            <div class="rounded-xl bg-white/5 border border-white/10 p-5">
                <div class="flex flex-wrap items-center gap-2 mb-2">
                    <code class="text-lg font-mono text-indigo-400">has_key(hash, key)</code>
                    <code class="text-lg font-mono text-indigo-400">delete(hash, key)</code>
                    <code class="text-lg font-mono text-indigo-400">merge(h1, h2)</code>
                </div>
                <p class="text-gray-400 mb-3">Check, delete, and merge hash operations.</p>
                <div class="rounded-lg bg-gray-950 ring-1 ring-white/10 p-3 overflow-x-auto">
<pre><code class="language-soli text-sm">has_key({"a": 1}, "a")    // true
delete({"a": 1, "b": 2}, "a")  // {"b": 2}
merge({"x": 1}, {"y": 2})     // {"x": 1, "y": 2}</code></pre>
                </div>
            </div>

            <div class="rounded-xl bg-white/5 border border-white/10 p-5">
                <div class="flex items-center gap-2 mb-2">
                    <code class="text-lg font-mono text-indigo-400">from_entries(array)</code>
                    <code class="text-lg font-mono text-indigo-400">clear(hash)</code>
                </div>
                <p class="text-gray-400 mb-3">Create hash from entries or clear a hash.</p>
                <div class="rounded-lg bg-gray-950 ring-1 ring-white/10 p-3 overflow-x-auto">
<pre><code class="language-soli text-sm">from_entries([["a", 1], ["b", 2]])  // {"a": 1, "b": 2}
let h = {"a": 1}
clear(h)  // h is now {}</code></pre>
                </div>
            </div>
        </div>

        <!-- String Functions -->
        <h3 class="text-xl font-semibold text-white mb-4 mt-8">String Functions</h3>

        <div class="space-y-6">
            <div class="rounded-xl bg-white/5 border border-white/10 p-5">
                <div class="flex flex-wrap items-center gap-2 mb-2">
                    <code class="text-lg font-mono text-indigo-400">split(str, sep)</code>
                    <code class="text-lg font-mono text-indigo-400">join(arr, sep)</code>
                </div>
                <p class="text-gray-400 mb-3">Split strings and join arrays.</p>
                <div class="rounded-lg bg-gray-950 ring-1 ring-white/10 p-3 overflow-x-auto">
<pre><code class="language-soli text-sm">split("a,b,c", ",")      // ["a", "b", "c"]
join(["a", "b"], "-")    // "a-b"</code></pre>
                </div>
            </div>

            <div class="rounded-xl bg-white/5 border border-white/10 p-5">
                <div class="flex flex-wrap items-center gap-2 mb-2">
                    <code class="text-lg font-mono text-indigo-400">contains(str, sub)</code>
                    <code class="text-lg font-mono text-indigo-400">index_of(str, sub)</code>
                    <code class="text-lg font-mono text-indigo-400">substring(str, start, end?)</code>
                </div>
                <p class="text-gray-400 mb-3">Search and extract from strings.</p>
                <div class="rounded-lg bg-gray-950 ring-1 ring-white/10 p-3 overflow-x-auto">
<pre><code class="language-soli text-sm">contains("hello", "ell")   // true
index_of("hello", "ll")    // 2
substring("hello", 1, 4)   // "ell"</code></pre>
                </div>
            </div>

            <div class="rounded-xl bg-white/5 border border-white/10 p-5">
                <div class="flex flex-wrap items-center gap-2 mb-2">
                    <code class="text-lg font-mono text-indigo-400">upcase(str)</code>
                    <code class="text-lg font-mono text-indigo-400">downcase(str)</code>
                    <code class="text-lg font-mono text-indigo-400">trim(str)</code>
                </div>
                <p class="text-gray-400 mb-3">Transform string case and whitespace.</p>
                <div class="rounded-lg bg-gray-950 ring-1 ring-white/10 p-3 overflow-x-auto">
<pre><code class="language-soli text-sm">upcase("hello")      // "HELLO"
downcase("HELLO")    // "hello"
trim("  hi  ")       // "hi"</code></pre>
                </div>
            </div>

            <div class="rounded-xl bg-white/5 border border-white/10 p-5">
                <div class="flex flex-wrap items-center gap-2 mb-2">
                    <code class="text-lg font-mono text-indigo-400">html_escape(str)</code>
                    <code class="text-lg font-mono text-indigo-400">html_unescape(str)</code>
                    <code class="text-lg font-mono text-indigo-400">sanitize_html(str)</code>
                </div>
                <p class="text-gray-400 mb-3">HTML encoding and security functions.</p>
                <div class="rounded-lg bg-gray-950 ring-1 ring-white/10 p-3 overflow-x-auto">
<pre><code class="language-soli text-sm">html_escape("&lt;script&gt;")     // "&amp;lt;script&amp;gt;"
html_unescape("&amp;lt;p&amp;gt;")  // "&lt;p&gt;"
sanitize_html("&lt;p onclick='x'&gt;") // "&lt;p&gt;"</code></pre>
                </div>
            </div>
        </div>

        <!-- File I/O Functions -->
        <h3 class="text-xl font-semibold text-white mb-4 mt-8">File I/O Functions</h3>

        <div class="space-y-6">
            <div class="rounded-xl bg-white/5 border border-white/10 p-5">
                <div class="flex items-center gap-2 mb-2">
                    <code class="text-lg font-mono text-indigo-400">slurp(path)</code>
                    <code class="text-lg font-mono text-indigo-400">barf(path, content)</code>
                </div>
                <p class="text-gray-400 mb-3">Read and write entire files.</p>
                <div class="rounded-lg bg-gray-950 ring-1 ring-white/10 p-3 overflow-x-auto">
<pre><code class="language-soli text-sm">let content = slurp("config.json")
barf("output.txt", "Hello, World!")</code></pre>
                </div>
            </div>
        </div>

        <!-- Math Functions -->
        <h3 class="text-xl font-semibold text-white mb-4 mt-8">Math Functions</h3>

        <div class="space-y-6">
            <div class="rounded-xl bg-white/5 border border-white/10 p-5">
                <div class="flex flex-wrap items-center gap-2 mb-2">
                    <code class="text-lg font-mono text-indigo-400">abs(n)</code>
                    <code class="text-lg font-mono text-indigo-400">min(a, b)</code>
                    <code class="text-lg font-mono text-indigo-400">max(a, b)</code>
                    <code class="text-lg font-mono text-indigo-400">sqrt(n)</code>
                    <code class="text-lg font-mono text-indigo-400">pow(base, exp)</code>
                </div>
                <p class="text-gray-400 mb-3">Common math operations.</p>
                <div class="rounded-lg bg-gray-950 ring-1 ring-white/10 p-3 overflow-x-auto">
<pre><code class="language-soli text-sm">abs(-5)       // 5
min(3, 7)     // 3
max(3, 7)     // 7
sqrt(16)      // 4.0
pow(2, 3)     // 8.0</code></pre>
                </div>
            </div>

            <div class="rounded-xl bg-white/5 border border-white/10 p-5">
                <div class="flex items-center gap-2 mb-2">
                    <code class="text-lg font-mono text-indigo-400">clock()</code>
                </div>
                <p class="text-gray-400 mb-3">Returns the current Unix timestamp as a float with sub-second precision.</p>
                <div class="rounded-lg bg-gray-950 ring-1 ring-white/10 p-3 overflow-x-auto">
<pre><code class="language-soli text-sm">let start = clock()
// ... do work ...
let elapsed = clock() - start</code></pre>
                </div>
            </div>
        </div>
    </section>

    <!-- HTTP Functions -->
    <section id="http" class="mb-16">
        <h2 class="text-3xl font-bold text-white mb-6 flex items-center gap-3">
            <span class="w-10 h-10 rounded-xl bg-green-500/20 flex items-center justify-center">
                <svg class="w-5 h-5 text-green-400" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 12a9 9 0 01-9 9m9-9a9 9 0 00-9-9m9 9H3m9 9a9 9 0 01-9-9m9 9c1.657 0 3-4.03 3-9s-1.343-9-3-9m0 18c-1.657 0-3-4.03-3-9s1.343-9 3-9m-9 9a9 9 0 019-9" />
                </svg>
            </span>
            HTTP Functions
        </h2>

        <div class="space-y-6">
            <div class="rounded-xl bg-white/5 border border-white/10 p-5">
                <div class="flex flex-wrap items-center gap-2 mb-2">
                    <code class="text-lg font-mono text-indigo-400">http_get(url, options?)</code>
                    <code class="text-lg font-mono text-indigo-400">http_post(url, body, options?)</code>
                </div>
                <p class="text-gray-400 mb-3">Perform HTTP GET and POST requests. Returns <code>{ "status": Int, "body": String, "headers": Hash }</code>.</p>
                <div class="rounded-lg bg-gray-950 ring-1 ring-white/10 p-3 overflow-x-auto">
<pre><code class="language-soli text-sm">let resp = http_get("https://api.example.com/users")
if resp["status"] == 200 {
    println(resp["body"])
}

let resp = http_post(
    "https://api.example.com/users",
    "name=Alice",
    { "headers": { "Content-Type": "application/x-www-form-urlencoded" } }
)</code></pre>
                </div>
            </div>

            <div class="rounded-xl bg-white/5 border border-white/10 p-5">
                <div class="flex flex-wrap items-center gap-2 mb-2">
                    <code class="text-lg font-mono text-indigo-400">http_get_json(url)</code>
                    <code class="text-lg font-mono text-indigo-400">http_post_json(url, data)</code>
                </div>
                <p class="text-gray-400 mb-3">JSON-specific HTTP methods with automatic serialization.</p>
                <div class="rounded-lg bg-gray-950 ring-1 ring-white/10 p-3 overflow-x-auto">
<pre><code class="language-soli text-sm">let data = http_get_json("https://api.example.com/users/1")
println(data["body"]["name"])

let resp = http_post_json(
    "https://api.example.com/users",
    { "name": "Alice", "email": "alice@example.com" }
)</code></pre>
                </div>
            </div>

            <div class="rounded-xl bg-white/5 border border-white/10 p-5">
                <div class="flex items-center gap-2 mb-2">
                    <code class="text-lg font-mono text-indigo-400">http_request(method, url, options?)</code>
                </div>
                <p class="text-gray-400 mb-3">Perform any HTTP method (GET, POST, PUT, PATCH, DELETE, etc.).</p>
                <div class="rounded-lg bg-gray-950 ring-1 ring-white/10 p-3 overflow-x-auto">
<pre><code class="language-soli text-sm">let resp = http_request("DELETE", "https://api.example.com/users/1")
let resp = http_request("PATCH", url, { "body": json, "headers": headers })</code></pre>
                </div>
            </div>

            <div class="rounded-xl bg-white/5 border border-white/10 p-5">
                <div class="flex flex-wrap items-center gap-2 mb-2">
                    <code class="text-lg font-mono text-indigo-400">http_ok(resp)</code>
                    <code class="text-lg font-mono text-indigo-400">http_success(resp)</code>
                    <code class="text-lg font-mono text-indigo-400">http_redirect(resp)</code>
                    <code class="text-lg font-mono text-indigo-400">http_client_error(resp)</code>
                    <code class="text-lg font-mono text-indigo-400">http_server_error(resp)</code>
                </div>
                <p class="text-gray-400 mb-3">Status code helpers: 200, 2xx, 3xx, 4xx, 5xx respectively.</p>
            </div>

            <div class="rounded-xl bg-white/5 border border-white/10 p-5">
                <div class="flex items-center gap-2 mb-2">
                    <code class="text-lg font-mono text-indigo-400">http_get_all(urls)</code>
                    <code class="text-lg font-mono text-indigo-400">http_parallel(requests)</code>
                </div>
                <p class="text-gray-400 mb-3">Parallel HTTP requests for better performance.</p>
                <div class="rounded-lg bg-gray-950 ring-1 ring-white/10 p-3 overflow-x-auto">
<pre><code class="language-soli text-sm">let responses = http_get_all([
    "https://api.example.com/users",
    "https://api.example.com/posts"
])

let responses = http_parallel([
    { "method": "GET", "url": "https://api.example.com/users" },
    { "method": "POST", "url": "https://api.example.com/logs", "body": "{}" }
])</code></pre>
                </div>
            </div>
        </div>
    </section>

    <!-- JSON Functions -->
    <section id="json" class="mb-16">
        <h2 class="text-3xl font-bold text-white mb-6 flex items-center gap-3">
            <span class="w-10 h-10 rounded-xl bg-amber-500/20 flex items-center justify-center">
                <svg class="w-5 h-5 text-amber-400" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 7v10c0 2.21 3.582 4 8 4s8-1.79 8-4V7M4 7c0 2.21 3.582 4 8 4s8-1.79 8-4M4 7c0-2.21 3.582-4 8-4s8 1.79 8 4" />
                </svg>
            </span>
            JSON Functions
        </h2>

        <div class="space-y-6">
            <div class="rounded-xl bg-white/5 border border-white/10 p-5">
                <div class="flex items-center gap-2 mb-2">
                    <code class="text-lg font-mono text-indigo-400">json_parse(string)</code>
                    <code class="text-lg font-mono text-indigo-400">json_stringify(value)</code>
                </div>
                <p class="text-gray-400 mb-3">Parse JSON strings and serialize Soli values to JSON.</p>
                <div class="rounded-lg bg-gray-950 ring-1 ring-white/10 p-3 overflow-x-auto">
<pre><code class="language-soli text-sm">let data = json_parse('{"name": "Alice", "age": 30}')
println(data["name"])  // Alice

let json = json_stringify({ "name": "Alice", "scores": [95, 87] })
println(json)  // {"name":"Alice","scores":[95,87]}</code></pre>
                </div>
            </div>
        </div>
    </section>

    <!-- Crypto Functions -->
    <section id="crypto" class="mb-16">
        <h2 class="text-3xl font-bold text-white mb-6 flex items-center gap-3">
            <span class="w-10 h-10 rounded-xl bg-red-500/20 flex items-center justify-center">
                <svg class="w-5 h-5 text-red-400" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 15v2m-6 4h12a2 2 0 002-2v-6a2 2 0 00-2-2H6a2 2 0 00-2 2v6a2 2 0 002 2zm10-10V7a4 4 0 00-8 0v4h8z" />
                </svg>
            </span>
            Cryptography Functions
        </h2>

        <h3 class="text-xl font-semibold text-white mb-4">Password Hashing</h3>
        <div class="space-y-6">
            <div class="rounded-xl bg-white/5 border border-white/10 p-5">
                <div class="flex flex-wrap items-center gap-2 mb-2">
                    <code class="text-lg font-mono text-indigo-400">argon2_hash(password)</code>
                    <code class="text-lg font-mono text-indigo-400">argon2_verify(password, hash)</code>
                </div>
                <p class="text-gray-400 mb-3">Hash passwords using Argon2id (recommended). Also available as <code>password_hash</code> and <code>password_verify</code>.</p>
                <div class="rounded-lg bg-gray-950 ring-1 ring-white/10 p-3 overflow-x-auto">
<pre><code class="language-soli text-sm">let hash = argon2_hash("secretpassword")
// $argon2id$v=19$m=19456,t=2,p=1$...

if argon2_verify(user_input, stored_hash) {
    println("Password correct!")
}</code></pre>
                </div>
            </div>
        </div>

        <h3 class="text-xl font-semibold text-white mb-4 mt-8">X25519 Key Exchange</h3>
        <div class="space-y-6">
            <div class="rounded-xl bg-white/5 border border-white/10 p-5">
                <div class="flex flex-wrap items-center gap-2 mb-2">
                    <code class="text-lg font-mono text-indigo-400">x25519_keypair()</code>
                    <code class="text-lg font-mono text-indigo-400">x25519_shared_secret(priv, pub)</code>
                </div>
                <p class="text-gray-400 mb-3">Generate key pairs and compute shared secrets for secure communication.</p>
                <div class="rounded-lg bg-gray-950 ring-1 ring-white/10 p-3 overflow-x-auto">
<pre><code class="language-soli text-sm">let alice = x25519_keypair()
let bob = x25519_keypair()

// Both compute the same shared secret
let alice_secret = x25519_shared_secret(alice["private"], bob["public"])
let bob_secret = x25519_shared_secret(bob["private"], alice["public"])</code></pre>
                </div>
            </div>
        </div>

        <h3 class="text-xl font-semibold text-white mb-4 mt-8">Ed25519 Signatures</h3>
        <div class="space-y-6">
            <div class="rounded-xl bg-white/5 border border-white/10 p-5">
                <div class="flex items-center gap-2 mb-2">
                    <code class="text-lg font-mono text-indigo-400">ed25519_keypair()</code>
                </div>
                <p class="text-gray-400 mb-3">Generate Ed25519 signing key pairs.</p>
            </div>
        </div>
    </section>

    <!-- JWT Functions -->
    <section id="jwt" class="mb-16">
        <h2 class="text-3xl font-bold text-white mb-6 flex items-center gap-3">
            <span class="w-10 h-10 rounded-xl bg-purple-500/20 flex items-center justify-center">
                <svg class="w-5 h-5 text-purple-400" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 7a2 2 0 012 2m4 0a6 6 0 01-7.743 5.743L11 17H9v2H7v2H4a1 1 0 01-1-1v-2.586a1 1 0 01.293-.707l5.964-5.964A6 6 0 1121 9z" />
                </svg>
            </span>
            JWT Functions
        </h2>

        <div class="space-y-6">
            <div class="rounded-xl bg-white/5 border border-white/10 p-5">
                <div class="flex items-center gap-2 mb-2">
                    <code class="text-lg font-mono text-indigo-400">jwt_sign(payload, secret, options?)</code>
                </div>
                <p class="text-gray-400 mb-3">Create a signed JWT token. Options: <code>expires_in</code> (seconds), <code>algorithm</code> (HS256, HS384, HS512).</p>
                <div class="rounded-lg bg-gray-950 ring-1 ring-white/10 p-3 overflow-x-auto">
<pre><code class="language-soli text-sm">let token = jwt_sign(
    { "sub": "user123", "role": "admin" },
    "my-secret-key",
    { "expires_in": 3600 }
)</code></pre>
                </div>
            </div>

            <div class="rounded-xl bg-white/5 border border-white/10 p-5">
                <div class="flex items-center gap-2 mb-2">
                    <code class="text-lg font-mono text-indigo-400">jwt_verify(token, secret)</code>
                </div>
                <p class="text-gray-400 mb-3">Verify and decode a JWT. Returns payload or <code>{ "error": true, "message": String }</code>.</p>
                <div class="rounded-lg bg-gray-950 ring-1 ring-white/10 p-3 overflow-x-auto">
<pre><code class="language-soli text-sm">let result = jwt_verify(token, "my-secret-key")
if has_key(result, "error") {
    println("Invalid token: " + result["message"])
} else {
    println("User: " + result["sub"])
}</code></pre>
                </div>
            </div>

            <div class="rounded-xl bg-white/5 border border-white/10 p-5">
                <div class="flex items-center gap-2 mb-2">
                    <code class="text-lg font-mono text-indigo-400">jwt_decode(token)</code>
                </div>
                <p class="text-gray-400 mb-3">Decode a JWT without verification (unsafe for auth, useful for inspection).</p>
            </div>
        </div>
    </section>

    <!-- Regex Functions -->
    <section id="regex" class="mb-16">
        <h2 class="text-3xl font-bold text-white mb-6 flex items-center gap-3">
            <span class="w-10 h-10 rounded-xl bg-pink-500/20 flex items-center justify-center">
                <svg class="w-5 h-5 text-pink-400" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z" />
                </svg>
            </span>
            Regex Functions
        </h2>

        <div class="space-y-6">
            <div class="rounded-xl bg-white/5 border border-white/10 p-5">
                <div class="flex items-center gap-2 mb-2">
                    <code class="text-lg font-mono text-indigo-400">regex_match(pattern, string)</code>
                </div>
                <p class="text-gray-400 mb-3">Test if a string matches a regex pattern. Returns Bool.</p>
                <div class="rounded-lg bg-gray-950 ring-1 ring-white/10 p-3 overflow-x-auto">
<pre><code class="language-soli text-sm">regex_match("^[a-z]+$", "hello")  // true
regex_match("^[0-9]+$", "hello")  // false</code></pre>
                </div>
            </div>

            <div class="rounded-xl bg-white/5 border border-white/10 p-5">
                <div class="flex items-center gap-2 mb-2">
                    <code class="text-lg font-mono text-indigo-400">regex_find(pattern, string)</code>
                    <code class="text-lg font-mono text-indigo-400">regex_find_all(pattern, string)</code>
                </div>
                <p class="text-gray-400 mb-3">Find matches. Returns <code>{ "match", "start", "end" }</code> or array of matches.</p>
                <div class="rounded-lg bg-gray-950 ring-1 ring-white/10 p-3 overflow-x-auto">
<pre><code class="language-soli text-sm">let result = regex_find("[0-9]+", "abc123def")
println(result["match"])  // "123"
println(result["start"])  // 3

let all = regex_find_all("[0-9]+", "a1b2c3")
// [{"match": "1"}, {"match": "2"}, {"match": "3"}]</code></pre>
                </div>
            </div>

            <div class="rounded-xl bg-white/5 border border-white/10 p-5">
                <div class="flex items-center gap-2 mb-2">
                    <code class="text-lg font-mono text-indigo-400">regex_replace(pattern, str, repl)</code>
                    <code class="text-lg font-mono text-indigo-400">regex_replace_all(pattern, str, repl)</code>
                </div>
                <p class="text-gray-400 mb-3">Replace first or all matches.</p>
                <div class="rounded-lg bg-gray-950 ring-1 ring-white/10 p-3 overflow-x-auto">
<pre><code class="language-soli text-sm">regex_replace("[0-9]+", "a1b2", "X")      // "aXb2"
regex_replace_all("[0-9]+", "a1b2", "X")  // "aXbX"</code></pre>
                </div>
            </div>

            <div class="rounded-xl bg-white/5 border border-white/10 p-5">
                <div class="flex items-center gap-2 mb-2">
                    <code class="text-lg font-mono text-indigo-400">regex_split(pattern, string)</code>
                    <code class="text-lg font-mono text-indigo-400">regex_capture(pattern, string)</code>
                    <code class="text-lg font-mono text-indigo-400">regex_escape(string)</code>
                </div>
                <p class="text-gray-400 mb-3">Split by regex, capture named groups, escape regex chars.</p>
                <div class="rounded-lg bg-gray-950 ring-1 ring-white/10 p-3 overflow-x-auto">
<pre><code class="language-soli text-sm">regex_split("[,;]", "a,b;c")  // ["a", "b", "c"]

let result = regex_capture("(?P&lt;year&gt;[0-9]{4})-(?P&lt;month&gt;[0-9]{2})", "2024-01-15")
println(result["year"])   // "2024"
println(result["month"])  // "01"

regex_escape("hello.world")  // "hello\\.world"</code></pre>
                </div>
            </div>
        </div>
    </section>

    <!-- Environment Functions -->
    <section id="env" class="mb-16">
        <h2 class="text-3xl font-bold text-white mb-6 flex items-center gap-3">
            <span class="w-10 h-10 rounded-xl bg-cyan-500/20 flex items-center justify-center">
                <svg class="w-5 h-5 text-cyan-400" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10.325 4.317c.426-1.756 2.924-1.756 3.35 0a1.724 1.724 0 002.573 1.066c1.543-.94 3.31.826 2.37 2.37a1.724 1.724 0 001.065 2.572c1.756.426 1.756 2.924 0 3.35a1.724 1.724 0 00-1.066 2.573c.94 1.543-.826 3.31-2.37 2.37a1.724 1.724 0 00-2.572 1.065c-.426 1.756-2.924 1.756-3.35 0a1.724 1.724 0 00-2.573-1.066c-1.543.94-3.31-.826-2.37-2.37a1.724 1.724 0 00-1.065-2.572c-1.756-.426-1.756-2.924 0-3.35a1.724 1.724 0 001.066-2.573c-.94-1.543.826-3.31 2.37-2.37.996.608 2.296.07 2.572-1.065z" />
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 12a3 3 0 11-6 0 3 3 0 016 0z" />
                </svg>
            </span>
            Environment Functions
        </h2>

        <div class="space-y-6">
            <div class="rounded-xl bg-white/5 border border-white/10 p-5">
                <div class="flex flex-wrap items-center gap-2 mb-2">
                    <code class="text-lg font-mono text-indigo-400">getenv(name)</code>
                    <code class="text-lg font-mono text-indigo-400">setenv(name, value)</code>
                    <code class="text-lg font-mono text-indigo-400">unsetenv(name)</code>
                    <code class="text-lg font-mono text-indigo-400">hasenv(name)</code>
                </div>
                <p class="text-gray-400 mb-3">Get, set, remove, and check environment variables.</p>
                <div class="rounded-lg bg-gray-950 ring-1 ring-white/10 p-3 overflow-x-auto">
<pre><code class="language-soli text-sm">let db_url = getenv("DATABASE_URL")
setenv("MY_VAR", "my_value")
unsetenv("MY_VAR")

if hasenv("DEBUG") {
    println("Debug mode enabled")
}</code></pre>
                </div>
            </div>

            <div class="rounded-xl bg-white/5 border border-white/10 p-5">
                <div class="flex items-center gap-2 mb-2">
                    <code class="text-lg font-mono text-indigo-400">dotenv(path?)</code>
                </div>
                <p class="text-gray-400 mb-3">Load environment variables from .env files. Without path, loads .env and .env.{APP_ENV}.</p>
                <div class="rounded-lg bg-gray-950 ring-1 ring-white/10 p-3 overflow-x-auto">
<pre><code class="language-soli text-sm">dotenv()                    // Loads .env and .env.{APP_ENV}
dotenv(".env.production")   // Loads specific file</code></pre>
                </div>
            </div>
        </div>
    </section>

    <!-- DateTime Functions -->
    <section id="datetime" class="mb-16">
        <h2 class="text-3xl font-bold text-white mb-6 flex items-center gap-3">
            <span class="w-10 h-10 rounded-xl bg-teal-500/20 flex items-center justify-center">
                <svg class="w-5 h-5 text-teal-400" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z" />
                </svg>
            </span>
            DateTime Functions
        </h2>

        <p class="text-gray-400 mb-6">Soli uses Unix timestamps (integers) for datetime operations. These are low-level functions prefixed with <code>__datetime_</code>.</p>

        <div class="space-y-6">
            <div class="rounded-xl bg-white/5 border border-white/10 p-5">
                <div class="flex flex-wrap items-center gap-2 mb-2">
                    <code class="text-lg font-mono text-indigo-400">__datetime_now_local()</code>
                    <code class="text-lg font-mono text-indigo-400">__datetime_now_utc()</code>
                </div>
                <p class="text-gray-400 mb-3">Get current time as Unix timestamp.</p>
            </div>

            <div class="rounded-xl bg-white/5 border border-white/10 p-5">
                <div class="flex flex-wrap items-center gap-2 mb-2">
                    <code class="text-lg font-mono text-indigo-400">__datetime_parse(string)</code>
                    <code class="text-lg font-mono text-indigo-400">__datetime_format(ts, fmt)</code>
                </div>
                <p class="text-gray-400 mb-3">Parse ISO 8601/RFC strings to timestamp, format timestamp to string.</p>
                <div class="rounded-lg bg-gray-950 ring-1 ring-white/10 p-3 overflow-x-auto">
<pre><code class="language-soli text-sm">let ts = __datetime_parse("2024-01-15T10:30:00Z")
__datetime_format(ts, "%Y-%m-%d %H:%M:%S")  // "2024-01-15 10:30:00"
__datetime_format(ts, "%B %d, %Y")           // "January 15, 2024"</code></pre>
                </div>
            </div>

            <div class="rounded-xl bg-white/5 border border-white/10 p-5">
                <div class="flex items-center gap-2 mb-2">
                    <code class="text-lg font-mono text-indigo-400">__datetime_components(ts)</code>
                </div>
                <p class="text-gray-400 mb-3">Get components: year, month, day, hour, minute, second, weekday, etc.</p>
                <div class="rounded-lg bg-gray-950 ring-1 ring-white/10 p-3 overflow-x-auto">
<pre><code class="language-soli text-sm">let parts = __datetime_components(ts)
println(parts["year"])     // 2024
println(parts["weekday"])  // "monday"</code></pre>
                </div>
            </div>

            <div class="rounded-xl bg-white/5 border border-white/10 p-5">
                <div class="flex flex-wrap items-center gap-2 mb-2">
                    <code class="text-lg font-mono text-indigo-400">__datetime_add(ts, secs)</code>
                    <code class="text-lg font-mono text-indigo-400">__datetime_sub(ts, secs)</code>
                    <code class="text-lg font-mono text-indigo-400">__datetime_diff(ts1, ts2)</code>
                </div>
                <p class="text-gray-400 mb-3">Arithmetic operations on timestamps.</p>
            </div>

            <div class="rounded-xl bg-white/5 border border-white/10 p-5">
                <div class="flex flex-wrap items-center gap-2 mb-2">
                    <code class="text-lg font-mono text-indigo-400">__datetime_is_before(ts1, ts2)</code>
                    <code class="text-lg font-mono text-indigo-400">__datetime_is_after(ts1, ts2)</code>
                    <code class="text-lg font-mono text-indigo-400">__datetime_to_iso(ts)</code>
                </div>
                <p class="text-gray-400 mb-3">Comparison and ISO conversion.</p>
            </div>
        </div>
    </section>

    <!-- Validation Functions -->
    <section id="validation" class="mb-16">
        <h2 class="text-3xl font-bold text-white mb-6 flex items-center gap-3">
            <span class="w-10 h-10 rounded-xl bg-emerald-500/20 flex items-center justify-center">
                <svg class="w-5 h-5 text-emerald-400" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z" />
                </svg>
            </span>
            Validation Functions
        </h2>

        <p class="text-gray-400 mb-6">Schema-based validation using the <code>V</code> class with chainable validators.</p>

        <div class="space-y-6">
            <div class="rounded-xl bg-white/5 border border-white/10 p-5">
                <div class="flex flex-wrap items-center gap-2 mb-2">
                    <code class="text-lg font-mono text-indigo-400">V.string()</code>
                    <code class="text-lg font-mono text-indigo-400">V.int()</code>
                    <code class="text-lg font-mono text-indigo-400">V.float()</code>
                    <code class="text-lg font-mono text-indigo-400">V.bool()</code>
                    <code class="text-lg font-mono text-indigo-400">V.array(schema?)</code>
                    <code class="text-lg font-mono text-indigo-400">V.hash(schema?)</code>
                </div>
                <p class="text-gray-400 mb-3">Create validators for different types.</p>
            </div>

            <div class="rounded-xl bg-white/5 border border-white/10 p-5">
                <h4 class="text-white font-semibold mb-2">Validator Chain Methods</h4>
                <p class="text-gray-400 mb-3">
                    <code>.required()</code>, <code>.optional()</code>, <code>.nullable()</code>, <code>.default(value)</code>,
                    <code>.min(n)</code>, <code>.max(n)</code>, <code>.pattern(regex)</code>, <code>.email()</code>, <code>.url()</code>
                </p>
            </div>

            <div class="rounded-xl bg-white/5 border border-white/10 p-5">
                <div class="flex items-center gap-2 mb-2">
                    <code class="text-lg font-mono text-indigo-400">validate(data, schema)</code>
                </div>
                <p class="text-gray-400 mb-3">Validate data against schema. Returns <code>{ "valid": Bool, "data": Hash, "errors": Array }</code>.</p>
                <div class="rounded-lg bg-gray-950 ring-1 ring-white/10 p-3 overflow-x-auto">
<pre><code class="language-soli text-sm">let schema = {
    "name": V.string().required().min(2),
    "email": V.string().required().email(),
    "age": V.int().optional().min(0).max(150)
}

let result = validate({
    "name": "Alice",
    "email": "alice@example.com"
}, schema)

if result["valid"] {
    println("Data is valid!")
} else {
    for error in result["errors"] {
        println(error["field"] + ": " + error["message"])
    }
}</code></pre>
                </div>
            </div>
        </div>
    </section>

    <!-- Session Functions -->
    <section id="session" class="mb-16">
        <h2 class="text-3xl font-bold text-white mb-6 flex items-center gap-3">
            <span class="w-10 h-10 rounded-xl bg-orange-500/20 flex items-center justify-center">
                <svg class="w-5 h-5 text-orange-400" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z" />
                </svg>
            </span>
            Session Functions
        </h2>

        <div class="space-y-6">
            <div class="rounded-xl bg-white/5 border border-white/10 p-5">
                <div class="flex flex-wrap items-center gap-2 mb-2">
                    <code class="text-lg font-mono text-indigo-400">session_get(key)</code>
                    <code class="text-lg font-mono text-indigo-400">session_set(key, value)</code>
                    <code class="text-lg font-mono text-indigo-400">session_delete(key)</code>
                </div>
                <p class="text-gray-400 mb-3">Read, write, and delete session values.</p>
                <div class="rounded-lg bg-gray-950 ring-1 ring-white/10 p-3 overflow-x-auto">
<pre><code class="language-soli text-sm">session_set("user_id", 123)
let user_id = session_get("user_id")
session_delete("user_id")</code></pre>
                </div>
            </div>

            <div class="rounded-xl bg-white/5 border border-white/10 p-5">
                <div class="flex flex-wrap items-center gap-2 mb-2">
                    <code class="text-lg font-mono text-indigo-400">session_destroy()</code>
                    <code class="text-lg font-mono text-indigo-400">session_regenerate()</code>
                    <code class="text-lg font-mono text-indigo-400">session_has(key)</code>
                    <code class="text-lg font-mono text-indigo-400">session_id()</code>
                </div>
                <p class="text-gray-400 mb-3">Session management: destroy session, regenerate ID (for security after login), check existence, get ID.</p>
                <div class="rounded-lg bg-gray-950 ring-1 ring-white/10 p-3 overflow-x-auto">
<pre><code class="language-soli text-sm">// After login
session_set("user_id", user["id"])
session_regenerate()  // Security: generate new session ID

// Logout
session_destroy()</code></pre>
                </div>
            </div>
        </div>
    </section>

    <!-- Testing Functions -->
    <section id="testing" class="mb-16">
        <h2 class="text-3xl font-bold text-white mb-6 flex items-center gap-3">
            <span class="w-10 h-10 rounded-xl bg-lime-500/20 flex items-center justify-center">
                <svg class="w-5 h-5 text-lime-400" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5H7a2 2 0 00-2 2v12a2 2 0 002 2h10a2 2 0 002-2V7a2 2 0 00-2-2h-2M9 5a2 2 0 002 2h2a2 2 0 002-2M9 5a2 2 0 012-2h2a2 2 0 012 2m-6 9l2 2 4-4" />
                </svg>
            </span>
            Testing Functions
        </h2>

        <h3 class="text-xl font-semibold text-white mb-4">Test DSL</h3>
        <div class="space-y-6">
            <div class="rounded-xl bg-white/5 border border-white/10 p-5">
                <div class="flex flex-wrap items-center gap-2 mb-2">
                    <code class="text-lg font-mono text-indigo-400">test(desc, fn)</code>
                    <code class="text-lg font-mono text-indigo-400">describe(desc, fn)</code>
                    <code class="text-lg font-mono text-indigo-400">it(desc, fn)</code>
                </div>
                <p class="text-gray-400 mb-3">Define tests and test groups. <code>context</code> and <code>specify</code> are aliases.</p>
                <div class="rounded-lg bg-gray-950 ring-1 ring-white/10 p-3 overflow-x-auto">
<pre><code class="language-soli text-sm">describe("Calculator", fn() {
    test("adds numbers", fn() {
        assert_eq(add(1, 2), 3)
    })

    it("subtracts numbers", fn() {
        assert_eq(subtract(5, 3), 2)
    })
})</code></pre>
                </div>
            </div>

            <div class="rounded-xl bg-white/5 border border-white/10 p-5">
                <div class="flex flex-wrap items-center gap-2 mb-2">
                    <code class="text-lg font-mono text-indigo-400">before_each(fn)</code>
                    <code class="text-lg font-mono text-indigo-400">after_each(fn)</code>
                    <code class="text-lg font-mono text-indigo-400">before_all(fn)</code>
                    <code class="text-lg font-mono text-indigo-400">after_all(fn)</code>
                </div>
                <p class="text-gray-400 mb-3">Setup and teardown hooks.</p>
            </div>

            <div class="rounded-xl bg-white/5 border border-white/10 p-5">
                <div class="flex items-center gap-2 mb-2">
                    <code class="text-lg font-mono text-indigo-400">pending()</code>
                    <code class="text-lg font-mono text-indigo-400">skip()</code>
                </div>
                <p class="text-gray-400 mb-3">Mark tests as pending or skip them.</p>
            </div>
        </div>

        <h3 class="text-xl font-semibold text-white mb-4 mt-8">Assertions</h3>
        <div class="space-y-6">
            <div class="rounded-xl bg-white/5 border border-white/10 p-5">
                <div class="flex flex-wrap items-center gap-2 mb-2">
                    <code class="text-lg font-mono text-indigo-400">assert(cond)</code>
                    <code class="text-lg font-mono text-indigo-400">assert_not(cond)</code>
                    <code class="text-lg font-mono text-indigo-400">assert_eq(a, b)</code>
                    <code class="text-lg font-mono text-indigo-400">assert_ne(a, b)</code>
                </div>
                <p class="text-gray-400 mb-3">Basic assertions for conditions and equality.</p>
            </div>

            <div class="rounded-xl bg-white/5 border border-white/10 p-5">
                <div class="flex flex-wrap items-center gap-2 mb-2">
                    <code class="text-lg font-mono text-indigo-400">assert_null(v)</code>
                    <code class="text-lg font-mono text-indigo-400">assert_not_null(v)</code>
                    <code class="text-lg font-mono text-indigo-400">assert_gt(a, b)</code>
                    <code class="text-lg font-mono text-indigo-400">assert_lt(a, b)</code>
                </div>
                <p class="text-gray-400 mb-3">Null checks and numeric comparisons.</p>
            </div>

            <div class="rounded-xl bg-white/5 border border-white/10 p-5">
                <div class="flex flex-wrap items-center gap-2 mb-2">
                    <code class="text-lg font-mono text-indigo-400">assert_match(str, pattern)</code>
                    <code class="text-lg font-mono text-indigo-400">assert_contains(coll, val)</code>
                    <code class="text-lg font-mono text-indigo-400">assert_hash_has_key(h, k)</code>
                    <code class="text-lg font-mono text-indigo-400">assert_json(str)</code>
                </div>
                <p class="text-gray-400 mb-3">Pattern matching, containment, and JSON validation.</p>
            </div>
        </div>

        <h3 class="text-xl font-semibold text-white mb-4 mt-8">Factory Functions</h3>
        <div class="space-y-6">
            <div class="rounded-xl bg-white/5 border border-white/10 p-5">
                <div class="flex flex-wrap items-center gap-2 mb-2">
                    <code class="text-lg font-mono text-indigo-400">Factory.define(name, data)</code>
                    <code class="text-lg font-mono text-indigo-400">Factory.create(name)</code>
                    <code class="text-lg font-mono text-indigo-400">Factory.create_with(name, overrides)</code>
                </div>
                <p class="text-gray-400 mb-3">Define and create test data factories.</p>
                <div class="rounded-lg bg-gray-950 ring-1 ring-white/10 p-3 overflow-x-auto">
<pre><code class="language-soli text-sm">Factory.define("user", {
    "name": "Test User",
    "email": "test@example.com"
})

let user = Factory.create("user")
let admin = Factory.create_with("user", { "role": "admin" })</code></pre>
                </div>
            </div>

            <div class="rounded-xl bg-white/5 border border-white/10 p-5">
                <div class="flex flex-wrap items-center gap-2 mb-2">
                    <code class="text-lg font-mono text-indigo-400">Factory.create_list(name, count)</code>
                    <code class="text-lg font-mono text-indigo-400">Factory.sequence(name)</code>
                    <code class="text-lg font-mono text-indigo-400">Factory.clear()</code>
                </div>
                <p class="text-gray-400 mb-3">Create multiple instances, get sequence numbers, clear factories.</p>
            </div>
        </div>
    </section>

    <!-- I18n Functions -->
    <section id="i18n" class="mb-16">
        <h2 class="text-3xl font-bold text-white mb-6 flex items-center gap-3">
            <span class="w-10 h-10 rounded-xl bg-violet-500/20 flex items-center justify-center">
                <svg class="w-5 h-5 text-violet-400" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 5h12M9 3v2m1.048 9.5A18.022 18.022 0 016.412 9m6.088 9h7M11 21l5-10 5 10M12.751 5C11.783 10.77 8.07 15.61 3 18.129" />
                </svg>
            </span>
            I18n Functions
        </h2>

        <div class="space-y-6">
            <div class="rounded-xl bg-white/5 border border-white/10 p-5">
                <div class="flex items-center gap-2 mb-2">
                    <code class="text-lg font-mono text-indigo-400">I18n.locale()</code>
                    <code class="text-lg font-mono text-indigo-400">I18n.set_locale(locale)</code>
                </div>
                <p class="text-gray-400 mb-3">Get and set the current locale.</p>
                <div class="rounded-lg bg-gray-950 ring-1 ring-white/10 p-3 overflow-x-auto">
<pre><code class="language-soli text-sm">println(I18n.locale())  // "en"
I18n.set_locale("fr")</code></pre>
                </div>
            </div>

            <div class="rounded-xl bg-white/5 border border-white/10 p-5">
                <div class="flex items-center gap-2 mb-2">
                    <code class="text-lg font-mono text-indigo-400">I18n.translate(key, locale?, translations?)</code>
                    <code class="text-lg font-mono text-indigo-400">I18n.plural(key, count, locale?, translations?)</code>
                </div>
                <p class="text-gray-400 mb-3">Translate strings and handle pluralization.</p>
                <div class="rounded-lg bg-gray-950 ring-1 ring-white/10 p-3 overflow-x-auto">
<pre><code class="language-soli text-sm">let translations = {
    "en.greeting": "Hello",
    "fr.greeting": "Bonjour",
    "en.items_zero": "No items",
    "en.items_one": "1 item",
    "en.items_other": "Many items"
}

I18n.translate("greeting", "fr", translations)  // "Bonjour"
I18n.plural("items", 0, "en", translations)     // "No items"</code></pre>
                </div>
            </div>

            <div class="rounded-xl bg-white/5 border border-white/10 p-5">
                <div class="flex flex-wrap items-center gap-2 mb-2">
                    <code class="text-lg font-mono text-indigo-400">I18n.format_number(n, locale?)</code>
                    <code class="text-lg font-mono text-indigo-400">I18n.format_currency(amount, currency, locale?)</code>
                    <code class="text-lg font-mono text-indigo-400">I18n.format_date(ts, locale?)</code>
                </div>
                <p class="text-gray-400 mb-3">Format numbers, currency, and dates according to locale.</p>
                <div class="rounded-lg bg-gray-950 ring-1 ring-white/10 p-3 overflow-x-auto">
<pre><code class="language-soli text-sm">I18n.format_number(1234.56, "en")          // "1234.56"
I18n.format_number(1234.56, "fr")          // "1234,56"
I18n.format_currency(1234.56, "USD", "en") // "$1,234.56"
I18n.format_date(ts, "en")                 // "01/15/2024"
I18n.format_date(ts, "fr")                 // "15/01/2024"</code></pre>
                </div>
            </div>
        </div>
    </section>

    <!-- See Also -->
    <div class="rounded-2xl bg-gradient-to-br from-indigo-500/10 via-purple-500/10 to-pink-500/10 border border-indigo-500/20 p-8">
        <h3 class="text-2xl font-bold text-white mb-3">Related Documentation</h3>
        <div class="grid grid-cols-1 sm:grid-cols-2 gap-4">
            <a href="/docs/soli-language" class="group block p-4 rounded-xl bg-white/5 border border-white/10 hover:bg-white/10 hover:border-indigo-500/50 transition-all">
                <h4 class="font-semibold text-white group-hover:text-indigo-400 transition-colors">Soli Language Reference &rarr;</h4>
                <p class="text-sm text-gray-400 mt-1">Core language syntax and features</p>
            </a>
            <a href="/docs/testing" class="group block p-4 rounded-xl bg-white/5 border border-white/10 hover:bg-white/10 hover:border-indigo-500/50 transition-all">
                <h4 class="font-semibold text-white group-hover:text-indigo-400 transition-colors">Testing Guide &rarr;</h4>
                <p class="text-sm text-gray-400 mt-1">Complete testing documentation</p>
            </a>
            <a href="/docs/validation" class="group block p-4 rounded-xl bg-white/5 border border-white/10 hover:bg-white/10 hover:border-indigo-500/50 transition-all">
                <h4 class="font-semibold text-white group-hover:text-indigo-400 transition-colors">Validation Guide &rarr;</h4>
                <p class="text-sm text-gray-400 mt-1">Input validation in depth</p>
            </a>
            <a href="/docs/authentication" class="group block p-4 rounded-xl bg-white/5 border border-white/10 hover:bg-white/10 hover:border-indigo-500/50 transition-all">
                <h4 class="font-semibold text-white group-hover:text-indigo-400 transition-colors">Authentication Guide &rarr;</h4>
                <p class="text-sm text-gray-400 mt-1">JWT authentication patterns</p>
            </a>
        </div>
    </div>
</div>
