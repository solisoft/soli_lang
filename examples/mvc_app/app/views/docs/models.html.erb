<div class="max-w-3xl mx-auto">
    <div class="mb-16">
        <h1 class="text-4xl font-bold tracking-tight text-white sm:text-5xl mb-6">
            <span class="bg-gradient-to-r from-indigo-400 via-purple-400 to-pink-400 bg-clip-text text-transparent">Models & ORM</span>
        </h1>
        <p class="text-xl text-gray-300 leading-8">
            Manage your application's data layer with type-safe database operations.
            SoliDB provides a lightweight, document-oriented interface that feels natural.
        </p>
    </div>

    <h2 class="text-2xl font-bold text-white mb-6">Defining Models</h2>
    <p class="text-gray-400 mb-6">Create models by extending the <code>Model</code> class and defining a schema.</p>

    <div class="rounded-xl bg-gray-950 ring-1 ring-white/10 overflow-hidden shadow-xl mb-12">
        <div class="px-4 py-2 border-b border-white/5 bg-white/5 flex items-center justify-between">
            <span class="text-xs text-gray-400 font-mono">app/models/user.soli</span>
        </div>
        <div class="p-4 overflow-x-auto">
<pre><code class="language-soli text-sm">class User extends Model {
    static {
        // Collection name (defaults to filename)
        this.collection = "users";

        // Schema definition
        this.fields = {
            "name": Field.string({
                required: true,
                min_length: 2
            }),
            "email": Field.string({
                required: true,
                unique: true,
                index: true
            }),
            "age": Field.int({ min: 0 }),
            "active": Field.bool({ default: true }),
            "created_at": Field.datetime()
        };
    }
}</code></pre>
        </div>
    </div>

    <h2 class="text-2xl font-bold text-white mb-6">Field Types</h2>
    <div class="grid grid-cols-1 md:grid-cols-2 gap-4 mb-12">
        <div class="p-4 rounded-xl bg-white/5 border border-white/10">
            <code class="text-indigo-300 font-bold">Field.string()</code>
            <p class="text-sm text-gray-400 mt-1">Text content. Options: <code>min_length</code>, <code>max_length</code>, <code>regex</code>.</p>
        </div>
        <div class="p-4 rounded-xl bg-white/5 border border-white/10">
            <code class="text-indigo-300 font-bold">Field.int() / Field.float()</code>
            <p class="text-sm text-gray-400 mt-1">Numeric values. Options: <code>min</code>, <code>max</code>.</p>
        </div>
        <div class="p-4 rounded-xl bg-white/5 border border-white/10">
            <code class="text-indigo-300 font-bold">Field.bool()</code>
            <p class="text-sm text-gray-400 mt-1">True/False flag. Option: <code>default</code>.</p>
        </div>
        <div class="p-4 rounded-xl bg-white/5 border border-white/10">
            <code class="text-indigo-300 font-bold">Field.datetime()</code>
            <p class="text-sm text-gray-400 mt-1">Timestamps and dates.</p>
        </div>
        <div class="p-4 rounded-xl bg-white/5 border border-white/10">
            <code class="text-indigo-300 font-bold">Field.array(type)</code>
            <p class="text-sm text-gray-400 mt-1">List of values. E.g., <code>Field.array(Field.string())</code>.</p>
        </div>
        <div class="p-4 rounded-xl bg-white/5 border border-white/10">
            <code class="text-indigo-300 font-bold">Field.reference("Model")</code>
            <p class="text-sm text-gray-400 mt-1">Foreign key relationship to another model.</p>
        </div>
    </div>

    <h2 class="text-2xl font-bold text-white mb-6">CRUD Operations</h2>
    
    <div class="space-y-8 mb-12">
        <div>
            <h3 class="text-lg font-semibold text-white mb-3 flex items-center gap-2">
                <span class="px-2 py-1 rounded bg-green-500/20 text-green-400 text-xs font-mono">CREATE</span>
                Insert Data
            </h3>
            <div class="rounded-xl bg-gray-950 ring-1 ring-white/10 overflow-hidden shadow-xl">
                <div class="p-4 overflow-x-auto">
<pre><code class="language-soli text-sm">let user = User.create({
    "name": "Alice Johnson",
    "email": "alice@example.com",
    "age": 28
});</code></pre>
                </div>
            </div>
        </div>

        <div>
            <h3 class="text-lg font-semibold text-white mb-3 flex items-center gap-2">
                <span class="px-2 py-1 rounded bg-blue-500/20 text-blue-400 text-xs font-mono">READ</span>
                Query Data
            </h3>
            <div class="rounded-xl bg-gray-950 ring-1 ring-white/10 overflow-hidden shadow-xl">
                <div class="p-4 overflow-x-auto">
<pre><code class="language-soli text-sm">// Find by ID
let user = User.find("abc123");

// Fluent query interface
let users = User.where("active", "==", true)
                .order("created_at", "desc")
                .limit(10)
                .find();</code></pre>
                </div>
            </div>
        </div>

        <div>
            <h3 class="text-lg font-semibold text-white mb-3 flex items-center gap-2">
                <span class="px-2 py-1 rounded bg-yellow-500/20 text-yellow-400 text-xs font-mono">UPDATE</span>
                Modify Data
            </h3>
            <div class="rounded-xl bg-gray-950 ring-1 ring-white/10 overflow-hidden shadow-xl">
                <div class="p-4 overflow-x-auto">
<pre><code class="language-soli text-sm">// Update single record
User.update("abc123", { "age": 29 });

// Bulk update
User.where("status", "==", "pending")
    .update({ "status": "active" });</code></pre>
                </div>
            </div>
        </div>

        <div>
            <h3 class="text-lg font-semibold text-white mb-3 flex items-center gap-2">
                <span class="px-2 py-1 rounded bg-red-500/20 text-red-400 text-xs font-mono">DELETE</span>
                Remove Data
            </h3>
            <div class="rounded-xl bg-gray-950 ring-1 ring-white/10 overflow-hidden shadow-xl">
                <div class="p-4 overflow-x-auto">
<pre><code class="language-soli text-sm">User.delete("abc123");</code></pre>
                </div>
            </div>
        </div>
    </div>

    <h2 class="text-2xl font-bold text-white mb-6">Relationships</h2>
    <p class="text-gray-400 mb-6">Define associations between your models.</p>

    <div class="rounded-xl bg-gray-950 ring-1 ring-white/10 overflow-hidden shadow-xl mb-12">
        <div class="px-4 py-2 border-b border-white/5 bg-white/5 flex items-center justify-between">
            <span class="text-xs text-gray-400 font-mono">app/models/post.soli</span>
        </div>
        <div class="p-4 overflow-x-auto">
<pre><code class="language-soli text-sm">class Post extends Model {
    static {
        this.fields = {
            "title": Field.string(),
            "author_id": Field.reference("User")
        };

        // Post belongs to a user
        this.belongs_to("author", "User");
    }
}

// Usage
let user = post.author();</code></pre>
        </div>
    </div>

    <h2 class="text-2xl font-bold text-white mb-6">Lifecycle Hooks</h2>
    <p class="text-gray-400 mb-6">Intercept model events to add custom logic.</p>

    <div class="rounded-xl bg-gray-950 ring-1 ring-white/10 overflow-hidden shadow-xl mb-12">
        <div class="p-4 overflow-x-auto">
<pre><code class="language-soli text-sm">static {
    // Hash password before saving
    this.before_create = fn(data) {
        if data["password"] {
            data["password_hash"] = hash(data["password"]);
            data.remove("password");
        }
        return data;
    };

    // Send email after creation
    this.after_create = fn(user) {
        Mailer.send_welcome(user["email"]);
    };
}</code></pre>
        </div>
    </div>

    <div class="my-8 p-6 rounded-xl bg-indigo-500/10 border border-indigo-500/20 mb-12">
        <h4 class="text-lg font-semibold text-indigo-400 mb-2 flex items-center gap-2">
            <svg class="h-5 w-5" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" d="M12 18v-5.25m0 0a6.01 6.01 0 001.5-.189m-1.5.189a6.01 6.01 0 01-1.5-.189m3.75 7.478a12.06 12.06 0 01-4.5 0m3.75 2.383a14.406 14.406 0 01-3 0M14.25 18v-.192c0-.983.658-1.854 1.5-2.269a6.002 6.002 0 00-2.25-10.428m0 0a6.002 6.002 0 012.25-10.428M14.25 18a6.002 6.002 0 01-2.25 10.428m2.25-10.428v-.192c0-.983-.658-1.854-1.5-2.269" />
            </svg>
            Pro Tip
        </h4>
        <p class="text-indigo-200/80 text-sm">
            Keep your models focused on data consistency and business rules. 
            Validation logic belongs here, not in the controller.
        </p>
    </div>

    <h2 class="text-2xl font-bold text-white mb-6">Next Steps</h2>
    <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
        <a href="/docs/controllers" class="group block p-6 rounded-xl bg-white/5 border border-white/10 hover:bg-white/10 hover:border-indigo-500/50 transition-all">
            <h3 class="text-lg font-semibold text-white mb-2 group-hover:text-indigo-400 transition-colors">Controllers &rarr;</h3>
            <p class="text-sm text-gray-400">Use your models to handle requests.</p>
        </a>
        <a href="/docs/middleware" class="group block p-6 rounded-xl bg-white/5 border border-white/10 hover:bg-white/10 hover:border-indigo-500/50 transition-all">
            <h3 class="text-lg font-semibold text-white mb-2 group-hover:text-indigo-400 transition-colors">Middleware &rarr;</h3>
            <p class="text-sm text-gray-400">Process requests before they reach your data.</p>
        </a>
    </div>
</div>
