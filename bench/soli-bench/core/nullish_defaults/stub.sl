def with_default(x, default) {
    # TODO: use ??
    return default;
}

def coalesce(a, b, c) {
    # TODO: use ?? chain
    return c;
}

def safe_lookup(hash, key) {
    # TODO: use ??
    return "missing";
}
