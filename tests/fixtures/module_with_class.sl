export class Calculator {
    new() {
        this.result = 0;
    }

    fn add(a: Int, b: Int) -> Int {
        return a + b;
    }

    fn subtract(a: Int, b: Int) -> Int {
        return a - b;
    }
}
